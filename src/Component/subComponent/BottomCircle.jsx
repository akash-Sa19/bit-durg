import React from 'react'

const BottomCircle = (props) => {
  return (
    <div className={`w-4 h-4 bg-[${props.circleColor}] border-2 border-[${props.borderColor}]
     rounded-full`} >
        {
        }
    </div>
    
  )
}

export default BottomCircle
