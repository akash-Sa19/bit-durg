import React from 'react'

const BottomCircle = (props) => {
  return (
    <div className={`w-3 h-3 bg-[${props.circleColor}]
     rounded-full`} >
        {
        }
    </div>
    
  )
}

export default BottomCircle
